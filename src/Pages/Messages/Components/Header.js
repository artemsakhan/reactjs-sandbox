import UIConfig from "../../../UIConfig";

const Header = ({}) => {
    return (
        <div style={containerStyle}>
            Header
        </div>
    )
}

const containerStyle = {
    width: '100%',
    height: '80px',
};

export default Header;